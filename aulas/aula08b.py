from math import sqrt, floor
num = int(input('Enter a number: '))
root = sqrt(num)
print('The square root of {} is {:.2f}'.format(num, floor(root)))

# if you press Ctrl and Space ,from math import *(HERE)*, you you see all modules you can import from that library
# you can also do Ctrl and Space, import *(HERE)*, to see all librarys you can import
